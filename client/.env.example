# Vite proxies /api to the backend in dev (see vite.config.js), so no
# env variable is required for local development. Add VITE_API_URL here
# if you deploy frontend and backend separately and need an absolute URL.
VITE_API_URL=http://localhost:5000/api
